---
description: Убрать бэйдж бэклога из statusLine Claude Code. Восстанавливает прежнюю statusLine-команду (если была) либо удаляет statusLine целиком; удаляет wrapper-скрипт. Правит ~/.claude/settings.json (или $CLAUDE_CONFIG_DIR).
allowed-tools: Bash(python3:*)
---

Ниже — вывод движка бэклога по откату statusLine. Сырой вывод пользователю НЕ
показывается: он инжектится только в твой контекст. Пользователь увидит лишь то, что
ты сам напечатаешь в ответе.

Передай результат дословно: восстановлена ли прежняя команда, удалён ли statusLine
целиком, либо statusLine оказался чужим и его не тронули. Скажи, что изменения вступят
в силу в новой сессии Claude Code.

Если вывод начинается с `error:` — покажи текст ошибки и не заявляй, что всё готово.

!`python3 "${CLAUDE_PLUGIN_ROOT}/scripts/backlog.py" statusline uninstall 2>&1 || true`
