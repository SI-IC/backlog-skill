---
description: Показать бэклог проекта (по умолчанию open, сортировка важность→дата). Аргументы прокидываются в backlog.py list (напр. --all, --sort date, --priority high).
allowed-tools: Bash(python3:*)
---

Ниже — вывод движка бэклога. Этот сырой вывод пользователю НЕ показывается: он
инжектится только в твой контекст. Единственный бэклог, который увидит пользователь,
— тот, что ты сам напечатаешь в этом ответе.

Отрисуй его: выведи таблицу движка дословно в code-block (чтобы колонки не поехали),
затем одной строкой — сколько открытых задач. Не переформатируй, не выдумывай и не
опускай строки. НИКОГДА не пиши «бэклог показан выше», «см. выше» и т.п. — выше ничего
нет.

Если движок вернул «Бэклог пуст …» — так и передай, таблицу не рисуй.

!`python3 "${CLAUDE_PLUGIN_ROOT}/scripts/backlog.py" list $ARGUMENTS 2>&1 || true`
